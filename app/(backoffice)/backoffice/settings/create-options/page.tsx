import { redirect } from "next/navigation";

/** Canonical workspace: create-policy (U29). */
export default function BackofficeSettingsCreateOptionsRedirectPage() {
  redirect("/backoffice/settings/create-policy");
}
