"use client";

/**
 * Kanonisk sjåførflate for /driver — én operativ overflate (liste, status, bekreftelse).
 * Implementasjon: `DriverClient`.
 */
import DriverClient from "./DriverClient";

export default function DriverRuntimeClient() {
  return <DriverClient />;
}
