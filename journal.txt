FASE 9A — RLS-grants + membership-lifecycle (2026-05-13)

Problem:
- Inger (firmaadmin på Melhus) ble blokkert fra /admin/people, /locations,
  /orders, /insights med 503 Service Unavailable.
- Root cause: tre lag brukte tre ulike sannhetskilder for tenant-tilhørighet:
  * Appkode: profiles.company_id (dokumentert som "tenant source of truth")
  * Private RLS-helpere: company_memberships.status='active'
  * Legacy recompute-trigger: company_memberships.active=true (boolean)
- Symptom: status='invited' på alle 4 prod-memberships blokkerte RLS.

Deployet:
1. GRANT EXECUTE på 16 private RLS-helpere for authenticated-rolle
   (can_access_company, can_view_order, manage_location, etc.).
2. project_profile_scope_from_memberships() endret til å respektere
   status IN ('active', 'suspended') for company-scope.
3. sync_memberships_from_legacy_profile() endret til å sette status korrekt:
   - status='active' når profile aktiv, 'suspended' når deaktivert
   - active=true ALLTID (lifecycle eies av status, ikke active)
   - activated_at via COALESCE for å bevare original timestamp
   - Trigger lytter også på is_active-kolonne
4. Engangs-datafix for inger@melhuscatering.no:
   - UPDATE company_memberships SET status='active', activated_at=now()
   - Anvendt manuelt via Supabase MCP 12:54:33 UTC 2026-05-13
   - Berørte 1 rad

Skalerbarhet:
- Nye firma-admin-registreringer får automatisk status='active' via
  sync-trigger.
- Ingen manuell DB-intervensjon nødvendig for fremtidige onboardinger.

Arkitektur-prinsipper etablert:
- company_memberships.active = "raden eksisterer" (alltid TRUE)
- company_memberships.status = lifecycle (invited/active/suspended/revoked)
- profiles.company_id bevares for status IN ('active', 'suspended')
- RLS-helpere fortsetter å lese status='active'

Lærdom:
- Kjedetrigger-arkitekturen dokumentert (sync -> recompute -> sync-loop).
- Rollback-tester via Supabase MCP gjorde trygg redesign mulig.
- A/B-migrasjon ga isolert testbar fix.

Utestått teknisk gjeld:
- /admin/invite "Mangler firmatilknytning" (FASE 9B neste)
- Kitchen/driver/superadmin membership-rolle-mapping
- location_memberships lifecycle (legge til status-kolonne)
- Designfix /admin/people og /admin/insights
- Firma-admin lunsjbestilling (variant 3a)
- CTA-knapp fra /admin oversikt til /week

FASE 9B — /admin/invite canonical profile lookup (2026-05-13)

Problem:
- /admin/invite viste "Mangler firmatilknytning" for company_admin.
- actions.ts brukte .or(id.eq.<uid>,user_id.eq.<uid>) — user_id-kolonne
  finnes ikke i prod-schema.
- Query feilet med 42703, error path triggert.

Deployet (commit 5373d175):
- actions.ts: canonical .eq("id", userId) lookup.
- page.tsx: send companyId + companyName som props.
- SimpleEmployeeInvite: vis "Inviterer til <firma>" i UI.
- Differensierte feilkoder: PROFILE_LOOKUP_FAILED vs MISSING_COMPANY.

Config-fix:
- Vercel Production env: PUBLIC_APP_URL var satt til http://localhost:3000.
- Endret til https://app.lunchportalen.no.
- Trigger redeploy.
- Resultat: invite-URLer peker nå til prod-domene.

FASE 9C — InvitesPanel wiring (2026-05-13)

Problem:
- "Siste invitasjoner"-seksjon var tom selv med invitasjoner i DB.
- "Oppdater"-knapp reagerte ikke ved klikk.
- InvitesPanel ble rendret uten props (<InvitesPanel />).

Deployet (commit e5abfb83):
- InviteClient.tsx: fetcher /api/admin/invites på mount.
- Følger /admin/people-mønsteret med load() + useEffect.
- Refresh etter vellykket invite-send.
- Pass rows/loading/error/onReload til InvitesPanel.

ARKITEKTUR-INNSIKTER FRA DENNE SESJONEN
- 10 filer har samme .or(id.eq,user_id.eq) bug-mønster (teknisk gjeld).
- PUBLIC_APP_URL i Vercel manglet riktig verdi (config-svakhet).
- getPublicAppUrlFromEnv() har silent fallback til localhost (latent bug).

UTESTÅTT TEKNISK GJELD (PRIORITERT)

HØY PRIORITET (blokkerer full automatisk flyt):
1. SMTP-fix (queryA EBADNAME mail.lunchportalen.no)
   - Option A: Fiks DNS for mail.lunchportalen.no
   - Option B: Bytt til Resend (LP_RESEND_FROM allerede satt)
   - Option C: Annen e-post-tjeneste
2. NEXT_PUBLIC_APP_URL verdi i Vercel (sjekk og fiks om nødvendig)

MIDDELS PRIORITET:
3. 10 filer med .or(id.eq,user_id.eq)-mønster
   - kitchen, driver, agreement, support, onboarding routes
4. Defensiv kode-forbedring i getPublicAppUrlFromEnv
   (kaste feil i prod hvis localhost, ikke silent-fail)
5. Kitchen/driver/superadmin role-mapping

LAV PRIORITET (designforbedringer):
6. Designfix /admin/people (mørke kort)
7. Designfix /admin/insights (grønnlig bakgrunn)
8. Firma-admin lunsjbestilling (variant 3a)
9. CTA-knapp fra /admin oversikt til /week
10. location_memberships lifecycle (status-kolonne)

---

## 13.05.2026 — Sesjon kveld (fortsettelse av fase 9)

### COMMITS DEPLOYET (6)
- 92776952: fix(invites): nodemailer SMTP → Resend
- 53837845: fix(invites): resend også migrert + post@-mail
- 54eae52b: fix(accept-invite): canonical handler + nested email envelope
- e8645638: fix(accept-invite): bind profile to company + fjern disabled_reason
- 5b5b9194: fix(accept-invite): fjern name og department
- ae067e69: fix(week): canonical profile lookup (7 filer)

### RESEND-MIGRASJON [COMPLETED]
- DNS verifisert (DKIM, MX, SPF) hos Domene.no
- Domain status: Verified i Resend
- Region: Ireland (eu-west-1)
- API-key i Vercel env (Production+Preview)
- Test-e-post bekreftet levering til inbox

### ACCEPT-INVITE PROGRESJON
Mellomstegs-test (etter commit 5b5b9194, FØR ae067e69) viste at:
- Profile ble opprettet i DB med korrekt company_id og location_id
- Invite ble markert used_at
- Men /week redirected til /status?code=PROFILE_MISSING

Det skyldtes uscoped profiles.maybeSingle()-mønster i 7 /week-filer
(ae067e69 fix). Etter siste deploy + cleanup ble accept-flyten IKKE
re-testet end-to-end før sesjonsavslutning.

### IKKE-VERIFISERT ETTER SISTE DEPLOY
- Om accept-flyten nå redirecter til /week istedenfor /status?code=PROFILE_MISSING
- Om sofus898 kan logge inn manuelt etter accept

### ÅPNE PUNKTER FOR FASE 9J (i morgen)

PRIORITET 1 — VERIFISERING:
- Test invite-lenken end-to-end i fresh incognito
- Token: 337bf1de990a70606ec52215941167e4843a8c73ed5802dbe8a35914feb1d6da
- Forventet: registreringsside → "Aktiver konto" → /week (Melhus-data)
- Hvis feiler: ny diagnose
- Verifiser i DB at invite_used_at settes OG profile er korrekt scoped

PRIORITET 2 — KRITISK UX (besluttet 9H):
- app.lunchportalen.no/ rotside skal være ren server-side redirect
  - Anonym → /login
  - Ansatt → /week
  - Admin → /admin
  - Superadmin → /superadmin
- Slett app/(public)/page.tsx content, erstatt med redirect-logikk
- LogoutClient (HeaderShellView linje 59-66) skal kun rendre når email finnes
- Marketing-content (RelatedLinks) skal flyttes til Umbraco
  → Bryter hardregel "Markedsinnhold → Umbraco"

PRIORITET 3 — CSS-POLISH:
- /register/employee container: max-w-md → max-w-[540px]
- "Aktiver konto"-knapp: gjenbruke login-knappens svart/gradient-stil
  → Referanse: components/auth/LoginForm.tsx linje 167-170

PRIORITET 4 — TEKNISK GJELD:
- handle_new_user-trigger setter ikke metadata.company_id
  → Profile-binding gjøres eksplisitt i accept-invite handler
  → Kan migreres til trigger-basert binding senere
- Andre filer har sannsynligvis samme uscoped maybeSingle()-bug
  → Søk: kitchen, driver, agreement, support, onboarding routes
  → 10+ filer estimert
- profiles-schema har felter handler-koden ikke vet om
  → disabled_reason, name, department finnes IKKE
  → Mulig andre handlers har samme antakelse

### DB-STATE VED SESJONSAVSLUTNING
- sofus898: ryddet (profiles=0, auth.users=0, invite.unused=1)
- Invite token: 337bf1de... gyldig til ~22 timer fra cleanup-tid
- Inger: aktiv som admin på Melhus

### ARBEIDSDISIPLIN BEKREFTET
- BOM-fri commits via [System.IO.File]::WriteAllText
- A/B-fase-deling per commit-deploy-test
- SELECT-only verifikasjon før DELETE-transaksjoner
- Cleanup-transaksjon kjøres KUN etter Vercel deploy Ready
- Hver commit isolert nok til separat rollback

---

## 2026-05-14 — Audit: 25% MVA / 1.25 i kodebase (10C.3-FIX-PHASE-3-APPLY, INFO)

Kontekst: `company_current_agreement_rules`-VIEW bruker **15%** (`* 1.15`) for mat/catering. Andre stier bruker fortsatt ofte **25%** — ikke endret i denne fasen.

**Kontrakt / pris (eksplisitt 0.25 eller (1+vat) med default 0.25):**
- `app/api/onboarding/complete/route.ts` — `normalizeDays(..., vat_rate = 0.25)`, `price_inc_vat = roundNok(price_ex_vat * (1 + vat_rate))`, `vatRate` fallback `0.25` (ca. 147–170, 529).
- `app/onboarding/OnboardingForm.tsx` — `const vat = 0.25`, `price_inc_vat: ... (1 + vat)` (ca. 152–159, 188).
- `lib/registration/weekdayMealTiers.ts` — default `opts.vatRate` `0.25`, `pEx * (1 + vat)` (ca. 96–113).
- `lib/server/superadmin/createAgreementDraftFromRegistration.ts` — `vatRate: 0.25` (ca. 473).

**Øvrige treff på `1.25` i grep (ikke MVA):** typisk `1.25rem`, design-spacing, `rgba(...,0.25)`, vekter i AI/ML — irrelevant for moms.

**VIEW på prod:** `public.company_current_agreement_rules` opprettet med `round(price_ex_vat * 1.15)`; Melhus verifisert BASIS 90→104, LUXUS 130→150.

---

## 2026-05-14 → 2026-05-16 (over midnatt)

### Commits (16, kronologisk etter commit-tidsstempel)

1. da12237d — feat(cms): menuDay items[] med EU 1169/2011 presise allergener (26 oppføringer)
2. a7050ad5 — feat(week): vis menuDay items[] med ekspanderbar velger og allergen-badges
3. b35e7135 — chore: redeploy etter Sanity projectId-fiks til 4udoq5d8 (mismatch fra 4hl2r9he)
4. 7032cbc1 — feat(orders): persister elementvalg på radnivå i day_choices
5. 95f193c3 — refactor(week): felles category-card ekspander-UX på alle kategorier
6. 9b066842 — refactor(week): fjern unit_price fra employee-vinduet (API + UI)
7. 21ad4d28 — refactor(week): tre UI-rensk på /week
8. b8a538fb — fix(week): behold «Bestill lunsj» i kort, fjern sticky bunn-CTA
9. 3f1eb220 — refactor(api): fjern unit_price fra employee CancelResponse
10. becf3acf — fix(superadmin): loadProductionReadiness alignet med reelt closed_dates-skjema
11. b281a4d3 — fix(superadmin): fail-soft når tabell company_billing_accounts mangler
12. 160939ba — fix(orders): instrumentering av writeOrder-feilstier for diagnose
13. 9d10baf7 — fix(week): ekspanderpanel ved valgt kategori + toggler lukket
14. 5bb7a511 — fix(db): opprett manglende view company_current_agreement_rules på prod
15. 1fcbac1c — fix(db): null-sikker avtalevindu i orders hydrate-trigger (starts_at etc.)
16. 557c2498 — fix(db): tg_order_defaults coalesce service_date fra date (RPC-/INSERT-gap)

### 500-saga — avdekkede lag

- Lag 1: Sanity projectId-mismatch (4hl2r9he → 4udoq5d8) — fikset/forankret deploy
- Lag 2: menuDay items[] manglet i Sanity-skjema + data — CMS + innhold lukket inn
- Lag 3: AGREEMENT_RULE_LOOKUP_FAILED — company_current_agreement_rules-view manglet på prod — migrering/view (5bb7a511)
- Lag 4: starts_at NULL-bug i tg_orders_hydrate_core_fields — fikset (1fcbac1c)
- Lag 5: service_date NULL-bug i tg_order_defaults — fikset (557c2498)
- Lag 6 (åpen — blokkerer bestilling): menu_service_days tomt mangler opprettelsespipeline; i tillegg rapportert skjema-/sannhetsdrift mellom locations og company_locations på analysemiljø (se under)

### Diagnose lag 6 (prod-kritisk)

- Tabell menu_service_days: 0 rader totalt på diagnostiserte miljøer (ordering feiler før trigger finner meny-dag).
- Fremmednøkkel menu_service_days.location_id peker på public.locations; denne kan være tom eller ikke speile reell operative lokasjon hvis drift bruker company_locations som autoritativ liste.
- app/api/superadmin/menu-publish: audit-stub uten faktisk publiserings-/DB-logikk til menu_service_days.
- Verken cron, Sanity-webhook eller synlig applikasjons-/migrationskode setter inn rader i menu_service_days på det spor som er gjennomgått (trigger tg_menu_service_day_defaults normaliserer når INSERT først finner sted — ingest mangler).
- Konsekvens: ny install eller miljø uten legacy-seed kan ikke gjennomføre kundedrevet bestilling; tg_order_defaults krever publisert/locked meny-dag på (location_id, service_date).

### Produksjonsklarhetsdiagnose (oppdatert)

- Kitchen-relaterte croner/reporting: kjent tema at aggregering er per ordre, ikke konsistent utlevert per item_key (operativ presisjon).
- Skjemadrift: locations vs company_locations, agreement_json-varianter, profiles.user_id vs id-mønstre mv. bør ryddes i egen fase.
- MVA / pris-stier: fortsatt 25 % i flere firmapåvirkede filer (onboarding/registrering) mot 15 %-VIEW i kontraktregler — målrettede 4-fil endring (±15 %) står på listen.
- E2E: suite finnes; full bestillingskorridor end-to-end er ikke dekning nok før lag 6 lukkes arkitekturmessig.
- Lag 6 tilsier vesentlig grunnarbeid (ordre av størrelsesorden 2–4 uker) før bestilling kan regnes stabilt på tvers av aktive lokasjoner — «50K firma / 500K brukere» ramme krever betydelig mer enn kortsiktige plaster; lag 6 bekrefter at fundament for meny-dagers speil må inn før skalering.

### PENDING (neste økt, prioritert)

- P0 — KRITISK: Lag 6 — menu_service_days-arkitektur: avklar en sannhet for lokasjon (locations vs company_locations); bygg publish-pipeline (Sanity/supplement → menu_service_days [+ evt. menu_service_day_items]); implementer reell POST /api/superadmin/menu-publish eller tilsvarende kontrollpunkt.
- P1 — Skjemadrift opprydning (agreement_json, profiles-lookup-mønstre, m.fl.).
- P2 — 15 % MVA i de fire registrerte kodestiene (fra 2026-05-14-audit).
- P3 — Kitchen-cron/strøm: aggregasjon/forsterkning ned til item_key der operasjonelt påkrevd.
- P4 — Onboarding E2E-test.
- P5 — Skalering-prep etter at P0 bestillingsfundament er dokumentert og grønn.

---

## 2026-05-16 — FASE 13-IMPL-3S-1-CLOSE (meny cron/CLI + overlevering)

### Commit (amend + push)

- **365897d1** (amend av 374da6f3) på `main`, pushet til `origin/main` (preflight-kjørte via git-hook før push).
- Commit-melding rettet: **sanityServer** / prod er koblet til **getSanityWriteToken()** i `lib/config/env.ts`, ikke et ikke-eksisterende `getSanityReadToken()`.

### Leveranse (kort)

- CLI-paritet: `scripts/cron-menu-publish.ts` og `scripts/sanity/heal-menu-horizon.ts` leser Sanity med token og `useCdn: false` i tråd med `sanityServer`; uten token ga ACL-datasett tom mealIdea-bank og «minimum 50 retter»-feil feildiagnostisert som sesong-vokabular.
- `mealIdeaBankQuery.ts`: `estimatedCostPerPortion <= 90` (off-by-one mot skjema max).
- `runMenuWeekRolloutCore.ts`: `dryRun` og `clock` for deterministisk replay.
- Diagnose: `scripts/debug-meal-idea-bank.ts`, `scripts/debug-v2-only.ts`, `scripts/find-empty-week.ts` + `npm run sanity:find-empty-week`.

### Verifikasjon / åpent: ukegenerator

- `sanity:find-empty-week`: mandager **2026-06-08 → 2026-08-31** — **0/5** varmrett for både BASIS og LUXUS (sommerkorridor uten `menuDay` i Sanity; uke **2026-06-01** var allerede fylt).
- `npm run cron:menu-publish -- --target-week 2026-06-08 --dry-run`: **dag 2** feiler (`Kunne ikke fylle dag 2…`) med lesbar pool — neste fase **13-IMPL-3S-1B-GENERATOR** (pickBest / variasjonsregler).

### Overlevering

- **Lukket på `main`:** token/CLI-paritet, kosttak, verktøy — commit **365897d1**.
- **Gjenstår:** generator når uken mangler `menuDay` og bank er ikke-tom.

---

## 2026-05-16 — FASE 13-IMPL-3S-1B-GENERATOR (kveld)

- **Commit `09f945bb`:** `lib/menu-publish/tagTaxonomy.ts` + meningsfull tag-overlap + `pickBestWithFallback`; telemetri `[LP_MENU_GENERATOR_RELAX]`; `scripts/diag-mealidea-tags.ts`; tester.
- **Verifisert** (`cron:menu-publish --dry-run`, `LP_MENU_GENERATOR_DEBUG=1`): **2026-05-25**, **2026-06-08**, **2026-06-15**, **2026-07-06** — hver **10 / 0 / []** (5 dager × 2 tier).
- **Relax-bruk:** **0** logger per uke, **0** totalt over 4 kjøringer («fix» bekreftet — streng regime holdt på prod-data uten fallback).
- Sommerkorridor **2026-06-08 → 2026-08-31** kan fylles autonomt av cron (forutsetter aktive avtaler/tier som i dag).
- **V2-oppfølging (valgfri):** migrere/rydde tag-bag i Sanity og redusere duplikat-encoding.

(Speilet innhold: `docs/journal.txt`.)

---

## 2026-05-17 — FASE 13.5-FIX-1-REPAIR (locations → company_locations)

- Commit **`387dfe34eb947c2ad5646ba15ef10bb429dd4be8`** (kort `387dfe34`).
- Kirurgisk repair av **private.can_access_location** og **private.can_manage_location**: `public.locations` → `public.company_locations` (migrasjon `20260516320000_repair_locations_helpers.sql`).
- Apply via Supabase MCP til prod (`hkpokyapzarefrgqzkos`); smoke test grønn for direkte kall (false på dummy-UUID) og indirekte (`can_access_menu_day`, `can_view_order`, `can_edit_order`, `can_access_delivery_run`, `can_operate_delivery_run`, `can_manage_menu_day`).
- **md5(pg_get_functiondef)** etter repair: `can_access_location` = `91583b4f512727d6fbf1b91f419ddb94`; `can_manage_location` = `96b9d18efea47841976ab4a514f9d586` (avviker fra pre-repair-hashene).
- Capture-migrasjon `20260517000000_capture_prod_rls_drift.sql` regenerert fra oppdatert `scripts/generate-prod-rls-capture.mjs`; `tests/rls/golden-rls-snapshot.json` utvidet med `capturedPrivateFunctionDefMd5` for de to funksjonene.
- **Fra 2026-06-08 og utover:** RLS-evaluering for **authenticated** skal igjen kunne bruke helpers uten 42P01 på `delivery_runs`, `location_closed_dates`, `location_policies`, `menu_service_days`, `orders`, `standing_orders` (direkte og kjede).
- STEG 6 (kjør capture som no-op mot prod): utelatt for å minimere prod-touch; capture speiler nå reparert helper-kode i embedded snapshot.

---

## 2026-05-17 — FASE 13.5-FIX-2-IMPL (HTTP-idempotency POST /api/orders)

- **Feature-commit `e13eb1801e4429e53526a56ca6f479e94b0e430e`** (kort **`e13eb180`**): migrasjon `20260516330000_extend_lp_idem_begin_status_code.sql`, `app/api/orders/route.ts`, `tests/api/orders-idempotency.test.ts` (9 nye tester).
- **DB:** `lp_idem_begin` utvidet med `status_code` slik at replay returnerer samme HTTP-status som opprinnelig suksess (f.eks. 201 når `p_response_code` var 201).
- **API:** valgfri `Idempotency-Key` (min. 8 tegn), `lp_idem_begin` / `lp_idem_complete` / `lp_idem_fail`, kanonisk hash først etter at alle felt som sendes til `lp_order_set` er kjent; `23505` → 409 `DUPLICATE_ORDER` med eksplisitt `23514`-eksklusjon i meldings-fallback; replay bruker `status_code ?? 200`; `IDEMPOTENCY_CACHE_INVALID`; cancel uten `orderId` kaller `lp_idem_complete`; `lp_idem_fail` kun med nøkkel + `request_hash`.
- **Journalføring + synk-JSDoc:** `journal.txt`, `docs/journal.txt` (speilet) og `lib/http/respond.ts` — levert i påfølgende commit på `main` rett etter feature-commit (se `git log`).

(Speilet innhold: `docs/journal.txt`.)

---

## 2026-05-16 — FASE 13.5-FIX-2B (UI sender Idempotency-Key på POST /api/orders)

- Klient: `lib/orders/idempotencyKey.ts` (`generateIdempotencyKey`), `app/(app)/week/EmployeeWeekClient.tsx`, `components/orders/OrderActions.tsx` — hver `POST` til `/api/orders` sender header `Idempotency-Key`; eksisterende pending-/in-flight-guards uendret.
- Tester: `tests/orders/idempotencyKey.test.ts`, `tests/orders/postOrdersClientIdempotency.test.ts`.
- Bygger på server-støtte levert i **FASE 13.5-FIX-2-IMPL** (`e13eb180`).

(Speilet innhold: `docs/journal.txt`.)

---

## 2026-05-16 — FASE 13.5-FIX-4 (CI-guard mot RLS-drift)

- **`scripts/check-rls-drift.mjs`:** READ-ONLY introspeksjon mot `pg_policies` / `pg_proc` vs `tests/rls/golden-rls-snapshot.json` — L1 policy-nøkler (stram mengde), L2 `private.*` signaturer (stram mengde), L3 `md5(pg_get_functiondef(oid))` for nøkler i `capturedPrivateFunctionDefMd5`; exit **0** match / **1** drift / **2** konfig eller tilkobling.
- **`.github/workflows/rls-drift-check.yml`:** planlagt kjøring daglig **06:00 UTC** + `workflow_dispatch`; secret **`DATABASE_URL`** (direkte Postgres, typisk port **5432**; skriptet sikrer `sslmode=require` ved behov).
- **`tests/rls/README.md`** + **`package.json`** (`check:rls-drift`).
- **Motivasjon:** tilsvarende brudd som i FIX-1-VERIFY (helpers mot feil tabell) kan ellers gå uoppdaget til produksjonssymptomer.
- **Aktivering:** etter merge må **`DATABASE_URL`** settes i GitHub repo settings; første verifikasjon via **workflow_dispatch** (forventer grønn L3 for `can_access_location` og `can_manage_location` når prod matcher golden).

(Speilet innhold: `docs/journal.txt`.)

---

## 2026-05-16 — FASE 13.5-FIX-4-FOLLOWUP (SSL til Supabase Session Pooler i RLS drift-check)

- **Problem:** CI-guard (`rls-drift-check.yml`) feilet med `DATABASE_ERROR` / «self-signed certificate in certificate chain» mot Supabase **Session Pooler** (`aws-0-eu-west-1.pooler.supabase.com`). Pooler-TLS-kjeden er ikke fullt ut tillit i Node's standard CA-store; nyere `pg` behandler `sslmode=require` som strengere verifikasjon.
- **Fix:** `scripts/check-rls-drift.mjs` — `pg.Pool` får `ssl: { rejectUnauthorized: false }`. Tilkoblingen forblir kryptert (TLS); bare CA-validering hoppes over for kjent pooler-vert. `ensureSslRequire()` beholdes (sikrer `sslmode=require` i URL).
- **Dokumentasjon:** `tests/rls/README.md` — Session Pooler + IPv4 for Actions vs direkte `db.*` (IPv6-only).

(Speilet innhold: `docs/journal.txt`.)

---

## 2026-05-16 — FASE 13.5-FIX-3-FOLLOWUP (fjern /api/orders/upsert)

- Slettet død rute `app/api/orders/upsert` (prod manglet `idem_get`/`idem_put`; kanon er POST `/api/orders` med `lp_idem_*`).
- Fjernet klient `upsertOrder` og hook `lib/orders/useOrderActions.ts`; kvitteringstyper for `ReceiptBanner` flyttet inn i komponenten (`OrderBannerAction`, `ReceiptState`, `ActionErrorState`).
- Oppdatert `lib/system/routeRegistry.ts` (fjernet `orders-upsert-post`), `lib/observability/sloRegistry.ts`, `docs/SLO_ALERTING_RUNBOOK.md`; `tests/api/order-api-guards.test.ts` uten upsert-describe.

(Speilet innhold: `docs/journal.txt`.)

---

## 2026-05-16 — FASE 13.5-FIX-5 (Migrasjonskollisjon 20260204)

- Diagnose: fem filer delte versjons-prefix 20260204 i `supabase/migrations/` (audit_events, company_archive, mega_motor phase1/2/3). Lokal `supabase db reset` feilet med 23505 på `schema_migrations` unique constraint. Prod hadde INGEN rader med `version='20260204%'` i `schema_migrations`, men hybrid objekt-tilstand (audit_events finnes, companies/profiles archive-kolonner finnes, mega_motor-tabeller finnes, men `company_deletions` + `claim_repair_jobs` + `repair_jobs_state_next_run_idx` manglet).
- Bruk-sjekk: `company_deletions` BRUKT av superadmin archive-summary route, `claim_repair_jobs` BRUKT av `superadmin/system/repairs/run`. Manglende objekter var faktiske bugs, ikke død kode.
- Fix:
  1. Rename 5 filer til unike timestamps (`20260204000001`–`5`) for å fjerne lokal kollisjon. Phase1+2 fikk samtidig BOM-fjerning pga Cursor Write-quirk.
  2. Ny migrasjon: `close_20260204_drift` med idempotent DDL for `company_deletions`, `claim_repair_jobs`, `repair_jobs_state_next_run_idx`.
  3. Apply mot prod via MCP (samme mønster som FIX-1/2). Verifisert alle tre objekter eksisterer i prod etter apply.
- Filnavn-justering: ny migrasjon renamet i repo til `20260516191414_close_20260204_drift.sql` for å matche prod `schema_migrations`-versjon.
- Workflow-status: `supabase-migrate.yml` vil ikke automatisk re-apply pga migration-gate blokkering på 20260513a/b (separat backlog). Prod er allerede synkronisert via MCP — workflow trenger ikke kjøre.
- Ingen selvrefererende SHA. Levert i denne commiten.

(Speilet innhold: `docs/journal.txt`.)

---

## 2026-05-16 — ECHO: session summary (kontinuitet)

- **`docs/session-summary-2026-05-16.md`:** samlet referanse for kveldens arbeid (ti kjernerevisjoner, manuelle GitHub/Supabase-tiltak uten hemmeligheter i teksten, prod-status, sikkerhetsnotater, backlog, skala-roadmap, Cursor-mønster). Ment for ny økt uten transkriptlesing.

(Speilet innhold: `docs/journal.txt`.)

