# Next.js
.next/
out/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# === .env hardening (Sprint AB Gr 8 — 2026-05-25) ===
# All .env-files except templates
.env.local.*
.env.vercel.local
.env.*.tmp
.env.k6-*
.env.prod-k6*
.env.sentry-diag-*
.env.staging-check*
.env.staging-pull*
.env.vercel.pull*
.env.preview-*

# Explicit keep (templates only)
!.env.example
!.env.postdeploy.example

# OS / Editor
.DS_Store
.vscode/
# Cursor IDE (local machine / agent state — not repo truth)
.cursor/

# Regenerable local inventory & audit dumps (do not commit)
full_audit.txt
fullAudit.json
dead-files.json
prioritizedTasks.json
structure.txt
docs/audit/sanity-dump/
scripts/audit/staging-schema-chunks/
scripts/audit/staging-schema-dump-2026-05-20.apply.sql
scripts/audit/staging-env-actual-*.env
scripts/seed/logs/

# Build artifacts
tsconfig.tsbuildinfo

# TS build info
tsconfig.tsbuildinfo
*.tsbuildinfo

.vercel

# Hyperscale local queue snapshot (file-based; override via LP_QUEUE_FILE)
queue.json

# Local temp / artifacts
.tmp/
tmp/
.backups/
# Root-only hyphenated temp files (e.g. curl/debug .tmp-*.html / .tmp-*.headers)
/.tmp-*
*.log
*.tsbuildinfo
.tmp_*.sql
.tmp_*.ts


# Evidence artifacts (generated)
evidence/

# Control-plane CI marker dump (regenerate via npm run verify:control-coverage)
reports/control-coverage-markers.json

# AI Video Studio — lokal ffmpeg / ElevenLabs-cache under public
public/generated/video-studio/
public/generated/voice-studio/

# Playwright (keep e2e/*-snapshots for visual baselines)
playwright-report/
test-results/
playwright/.cache/
blob-report/

# Python — cua/ policy merge tool
cua/**/__pycache__/
cua/**/*.egg-info/
cua/.venv/
cua/venv/
cua/.eggs/
cua/.pytest_cache/
cua/.coverage
cua/htmlcov/
cua/dist/
cua/build/
cua/*.egg-info/

Umbraco/bin/
Umbraco/obj/

# Umbraco local publish output
umbraco17/lunchportalen/publish/
umbraco17/lunchportalen/publish.zip
*.zip

# Local publish/build artifacts
artifacts/
build-output.txt
umbraco17/*BACKUP*/
.claude/

# Post-audit PF.1 (2026-05-26) — local evidence, not repo truth
.screenshots/
scripts/k6/results/
scripts/audit/*-dump-*.sql
*.dump.sql
