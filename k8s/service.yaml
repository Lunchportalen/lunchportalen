apiVersion: v1
kind: Service
metadata:
  name: lunchportalen-service
  labels:
    app: lunchportalen
spec:
  type: LoadBalancer
  selector:
    app: lunchportalen
  ports:
    - name: http
      port: 80
      targetPort: http
