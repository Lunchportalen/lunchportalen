"use client";

export default function AuditTable() {
  return (
    <div className="p-4">
      <h2 className="text-lg font-semibold">Audit</h2>
      <p className="mt-2 text-sm text-[rgb(var(--lp-muted))]">
        Audit-visning er koblet, men tabellvisning er ikke ferdigstilt ennå.
      </p>
    </div>
  );
}
