/**
 * Transitional barrel: canonical DS primitives live in `src/components/ui/ds`.
 * Prefer `@/components/ui/ds` (resolves via tsconfig to `src/` first).
 */
export * from "../../../src/components/ui/ds/index";
