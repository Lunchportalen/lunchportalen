"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `quote_block` */
export const RegistryQuoteBlockBlock = createRegistryBlock("quote_block");
