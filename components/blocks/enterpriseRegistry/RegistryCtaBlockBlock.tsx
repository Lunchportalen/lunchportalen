"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `cta_block` */
export const RegistryCtaBlockBlock = createRegistryBlock("cta_block");
