"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `hero_minimal` */
export const RegistryHeroMinimalBlock = createRegistryBlock("hero_minimal");
