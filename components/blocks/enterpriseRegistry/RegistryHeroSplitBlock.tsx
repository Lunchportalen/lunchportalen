"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `hero_split` */
export const RegistryHeroSplitBlock = createRegistryBlock("hero_split");
