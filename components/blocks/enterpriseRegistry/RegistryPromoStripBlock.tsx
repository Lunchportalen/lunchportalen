"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `promo_strip` */
export const RegistryPromoStripBlock = createRegistryBlock("promo_strip");
