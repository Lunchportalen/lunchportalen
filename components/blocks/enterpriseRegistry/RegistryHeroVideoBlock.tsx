"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `hero_video` */
export const RegistryHeroVideoBlock = createRegistryBlock("hero_video");
