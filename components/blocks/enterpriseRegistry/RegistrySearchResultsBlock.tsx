"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `search_results` */
export const RegistrySearchResultsBlock = createRegistryBlock("search_results");
