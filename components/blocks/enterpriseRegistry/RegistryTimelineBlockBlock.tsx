"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `timeline_block` */
export const RegistryTimelineBlockBlock = createRegistryBlock("timeline_block");
