"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `hero_bleed` */
export const RegistryHeroBleedBlock = createRegistryBlock("hero_bleed");
