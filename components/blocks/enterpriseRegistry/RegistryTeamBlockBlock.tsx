"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `team_block` */
export const RegistryTeamBlockBlock = createRegistryBlock("team_block");
