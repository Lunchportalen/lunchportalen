"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `menu_list` */
export const RegistryMenuListBlock = createRegistryBlock("menu_list");
