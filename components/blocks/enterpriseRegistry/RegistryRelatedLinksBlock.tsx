"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `related_links` */
export const RegistryRelatedLinksBlock = createRegistryBlock("related_links");
