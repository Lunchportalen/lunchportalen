"use client";

import { createRegistryBlock } from "@/components/blocks/EnterpriseLockedBlockView";

/** Enterprise CMS registry block: block.type `dynamic_feed` */
export const RegistryDynamicFeedBlock = createRegistryBlock("dynamic_feed");
