# Archive

Files moved here are **not part of the active static import graph** (see `audit-v4.cjs` / `dead-files.json`).
They are kept for reference or future salvage. Do not import from `archive/` in production code without moving the module back.
