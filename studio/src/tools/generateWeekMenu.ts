/**
 * Shared week-menu generator (Next.js `lib/menu-publish`).
 * Uses relative imports so Studio build + schema deploy work without `@/` (MP2).
 */
export * from "../../../lib/menu-publish/generateWeekMenu";
