# CI + local install: skip optional GPU ONNX binaries from api.nuget.org (bundled CPU runtime remains).
# See onnxruntime-node/script/install.js — ONNXRUNTIME_NODE_INSTALL=skip
onnxruntime-node-install=skip
