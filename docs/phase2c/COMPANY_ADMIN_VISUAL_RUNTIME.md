# Company admin — Visuell runtime (2C1)

## Prinsipp

- **Eksisterende primitives:** `lp-card`, `lp-btn`, `AdminPageShell`, `lp-h1` / `lp-h2`, rolig nøytral base + én primær handling der relevant.
- **Layout:** `max-w-[1440px]` på nav-container (AGENTS G7); `pt-[27px]` under header-gap (samme orden som canonical header-regel for admin shell).
- **Hot pink:** Kun på eksisterende primærknapp-mønstre — ingen nye store rosa flater.

## Endringer

- **AdminNav:** `rounded-2xl` faner, `min-h-[44px]` for touch, wrapping på mobil (ingen horisontal scroll-jam).

## Ikke gjort

- Full redesign av alle undersider — kun IA + oversikt + avtale-kort.
