# Provider meny visual — passthrough proof (RETUR-3)

Docs-only PR to prove `provider-meny-visual` required-check passthrough emits success when
provider-meny visual workflow paths are not touched.

Context name: `provider-meny-visual` (job `name:` in `required-checks-passthrough.yml`).
