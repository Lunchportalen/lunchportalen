# Utviklingsjournal (utdrag)

Eldre hendelser: se `journal.txt` i prosjektrot.

---

## 2026-05-14 → 2026-05-16 (over midnatt + neste morgen)

### Commits (28, kronologisk etter commit-tidsstempel)

1. 697af46a — fix(week): read per-day tier from agreement_delivery_days, not aggregated agreements.tier
2. 3abca6f3 — fix(week): two-week window — client uses weeks=2 as state source + cutoff 08:00 to 14:00
3. 200f298c — fix(week): show all days in selector (not just 5) + WCAG touch-target
4. 39657921 — fix(week): selector grid — one row per week, days in fixed weekday columns
5. 04d33546 — feat(week): 3-week overlap window via getVisibleWindow
6. b691c1b0 — fix(week): placeholder cells for past/unbookable days in selector grid
7. 9b500bb9 — fix(week): consolidate duplicate /week headers into one canonical block
8. c060ef78 — refactor(week): rename salatbar → salatboks across code, tests, docs
9. da12237d — feat(cms): menuDay items[] with EU 1169/2011 precise allergens (26 entries)
10. a7050ad5 — feat(week): render menuDay items[] with expanding picker and allergen badges
11. b35e7135 — chore: redeploy after Sanity projectId fix to 4udoq5d8
12. 7032cbc1 — feat(orders): persist item-level selection in day_choices
13. 95f193c3 — refactor(week): unify category-card expand UX across all categories
14. 9b066842 — refactor(week): remove unit_price from employee window response and UI
15. 21ad4d28 — refactor(week): three UI cleanups on employee /week surface
16. 8837c55d — fix(auth): improve contrast on "Aktiver konto" button
17. b8a538fb — fix(week): keep in-card "Bestill lunsj", remove sticky bottom CTA
18. 3f1eb220 — refactor(api): remove unit_price from employee CancelResponse
19. becf3acf — fix(superadmin): align loadProductionReadiness with actual closed_dates schema
20. b281a4d3 — fix(superadmin): fail-soft when company_billing_accounts table is absent
21. 160939ba — fix(orders): instrument writeOrder error paths for diagnostics
22. 9d10baf7 — fix(week): expand panel renders next to selected category + toggles closed
23. 5bb7a511 — fix(db): create missing company_current_agreement_rules view
24. 1fcbac1c — fix(db): null-safe agreement window in orders hydrate trigger
25. 557c2498 — fix(db): coalesce service_date from date in tg_order_defaults
26. e249c881 — docs(journal): kveldsoppdatering 14-16.05.2026 — 500-saga 5 av 6 lag fikset, lag 6 er arkitektur-grunnsak
27. 5786ab33 — feat(week): mark today's date + seed Sanity menuDay for uke 21-22
28. 2633ad21 — chore(seed): accept multiple env var names for Sanity write token

**Sesjonsavslutning (samme dokumentasjonscommit):** ny fil `docs/architecture/menu-publish-pipeline.md` (FASE 13-DESIGN); oppdatert denne `docs/journal.txt` — git-melding: `docs(journal): kveldsoppdatering 14-16.05.2026 — 500-saga 5/6 fikset, 12A visning OK, 13-design dokumentert`.

### 500-saga — 5 av 6 lag fikset (samme som tidligere journal-utkast)

- Lag 1: Sanity projectId-mismatch (4hl2r9he → 4udoq5d8) — fikset/forankret deploy
- Lag 2: menuDay items[] manglet i Sanity-skjema + data — CMS + innhold lukket inn
- Lag 3: AGREEMENT_RULE_LOOKUP_FAILED — `company_current_agreement_rules`-view manglet på prod — migrering/view (5bb7a511)
- Lag 4: starts_at NULL-bug i tg_orders_hydrate_core_fields — fikset (1fcbac1c)
- Lag 5: service_date NULL-bug i tg_order_defaults — fikset (557c2498)
- Lag 6 (åpen — blokkerer «full» bestilling til speil ligger i DB): `menu_service_days` mangler opprettelsespipeline; evt. skjema-/sannhetsdrift `locations` vs `company_locations` (FASE 13-design peker på `company_locations` som FK-sannhet — eierbeslutning før implementering)

### FASE 12A (morgen 16.05)

- Dagens dato markert i `/week` via kalender-pill (`ds-week-calendar-day-pill` + `--today`); tester oppdatert (`EmployeeWeekClient.test.tsx`, `employee-week.css`).
- **42** Sanity `menuDay`-dokumenter seedet for uke 21–22 (MIX-mønster) — ledd i commit 5786ab33; seed-Script støtter flere env-navn for write-token (2633ad21).

### FASE 13-DESIGN (ettermiddag 16.05)

- Leveranse: `docs/architecture/menu-publish-pipeline.md`
- Forankrede Defaults i design: **FK mot `company_locations`**, **hybrid webhook + reconcile-cron**, **månedlig declarative partisjon på `service_date`**, **trusted worker (UPSERT) — ikke Postgres direkte fra Sanity**, **08:00 Oslo** som utgangspunkt for `cutoff_at` / employee-cutoff (jf. `cutoffStatusForDate`), med eksplisitt advarsel om avvik mot 08:05-kjøkkenstier
- **Fem åpne arkitektur-beslutninger** venter eier før implementering:
  1. Cutoff per lokasjon vs global (og evt. 08:00 vs 08:05 canonical for brukerrettet lås)
  2. Horizont-dybde og SLA (dager frem, alarmnivå)
  3. Mal (`menuWeekTemplate`) vs «flate» `menuDay`-dokumenter som eneste sannhet
  4. Migrasjonsvei fra evt. `locations`-FK / tom `menu_service_days` uten nedetid
  5. Audit-mapping og kontrollpunkt for `menu-publish` vs kun worker/cron

### PENDING (neste økt — oppdatert prioritet)

- **P0 KRITISK:** Lag 6 — implementering etter FASE 13-design; **blokkert til eier har tatt de fem beslutningene over**
- **P1:** Schema-drift opprydning (`locations` / `company_profiles` / agreement_json-varianter mv.)
- **P2:** 15 % MVA-fix (fire registrerte kodestier)
- **P3:** Kitchen-cron — `item_key`-aggregering der operasjon krever det
- **P4:** Onboarding E2E-test
- **P5:** Skalering-prep (k6 loadtest etter at P0-grunnmur er grønn)

### 2026-05-16 (etter middag) — FASE 13-IMPL-3S-1-CLOSE

- **Commit `365897d1`** (amend + push `main`): meny **cron/CLI** Sanity-lesing med token (paritet med `sanityServer` / `getSanityWriteToken()` i `lib/config/env.ts`), `mealIdeaBankQuery` **<= 90**, `runMenuWeekRolloutCore` **dryRun/clock**, skript inkl. `find-empty-week` + `npm run sanity:find-empty-week`.
- **Tom sommerkorridor:** `find-empty-week` viste **0/5** varmrett Man–Fre for **2026-06-08 … 2026-08-31**; `cron:menu-publish --dry-run` for **2026-06-08** feiler på **dag 2** (generator/variasjon) — følge-fase **13-IMPL-3S-1B-GENERATOR**.
- Rotjournal: `journal.txt` (prosjektrot).

### 2026-05-16 (kveld) — FASE 13-IMPL-3S-1B-GENERATOR

- **Commit `09f945bb`:** meningsfull tag-overlap (`tagTaxonomy`), `pickBestWithFallback`, RELAX-telemetri, diag-skript, Vitest.
- **Verify:** dry-run uke **2026-05-25**, **06-08**, **06-15**, **07-06** — alle **10 / 0 / []**; **0** `LP_MENU_GENERATOR_RELAX` totalt.
- **V2 (valgfri):** Sanity tag-bag / strukturerte felt.

### Ærlig vurdering: 50K firma / 500K brukere / 20 år

- Designestimat: **~32M** `menu_service_days`-rader/år ved L ≈ 125K lokasjoner og ~260 servicedøgn — **partisjonering er obligatorisk**, ikke valgfri optimering.
- **Implementering av pipeline alene:** orden størrelsesorden **1–2 uker** fokusert arbeid etter eierbeslutninger (worker, upsert, reconcile, migreringsplan).
- **Full produksjonsklarhet** (overvåkning, last, RLS, kjøkkenflyt, regressjonstester): **måneder og personuker** — bekreftet som realistisk ramme, ikke «neste sprint».

### Diagnose lag 6 (samme kjerne som tidligere)

- Tabell `menu_service_days`: kan være tom uten pipeline — ordre-trigger finner ingen rad for `(location_id, service_date)`.
- `POST /api/superadmin/menu-publish` har vært audit-stub uten faktisk speil til DB inntil Lag 6 leveres.
- Konsekvens: miljø uten manuell legacy-seed kan ikke fullføre kundedrevet bestilling der DB-gate krever publisert meny-dag.

—

*Sist oppdatert: kveld 16.05.2026 (FASE 13-IMPL-3S-1B-GENERATOR + speil).*

---

## 2026-05-17 — FASE 13.5-FIX-1-REPAIR (locations → company_locations)

- Commit **`387dfe34eb947c2ad5646ba15ef10bb429dd4be8`** (kort `387dfe34`).
- `20260516320000_repair_locations_helpers.sql`: **CREATE OR REPLACE** for `private.can_access_location` og `private.can_manage_location` (én linje: join mot `public.company_locations`).
- Prod: apply via MCP; smoke grønn (direkte + indirekte helpers); nye def-md5 som i `golden-rls-snapshot.json` (`capturedPrivateFunctionDefMd5`).
- Capture `20260517000000` + generator oppdatert; ingen ekstra prod-kjøring av capture-fila (valgfritt STEG 6 hoppet).
- Sommerkorridor **2026-06-08+**: merk at meny/RLS-kjeden nå ikke skal dø på manglende `public.locations`.

*Speil: `journal.txt` (rot).*

---

## 2026-05-17 — FASE 13.5-FIX-2-IMPL (HTTP-idempotency POST /api/orders)

- **Feature-commit `e13eb1801e4429e53526a56ca6f479e94b0e430e`** (kort **`e13eb180`**): migrasjon `20260516330000_extend_lp_idem_begin_status_code.sql`, `app/api/orders/route.ts`, `tests/api/orders-idempotency.test.ts` (9 tester).
- `lp_idem_begin` + `status_code`; valgfri header; `23505` → `DUPLICATE_ORDER`; replay-status; defensiv cache-validering; CANCEL uten `orderId` fullfører idempotency.
- **Journalføring:** speilet i `journal.txt` (rot) og denne filen; `jsonOrderWriteOk` har krysshenvisning til `buildOrderWriteOkJsonBody` i `app/api/orders/route.ts`. Levert i commit på `main` rett etter `e13eb180` (se `git log`).

*Speil: `journal.txt` (rot).*

---

## 2026-05-16 — FASE 13.5-FIX-2B (UI sender Idempotency-Key på POST /api/orders)

- `generateIdempotencyKey` i `lib/orders/idempotencyKey.ts`; uke-UI og `OrderActions` sender `Idempotency-Key` på `POST /api/orders`; Vitest for helper + kilde-kontrakt.
- Server: **FASE 13.5-FIX-2-IMPL** (`e13eb180`).

*Speil: `journal.txt` (rot).*

---

## 2026-05-16 — FASE 13.5-FIX-4 (CI-guard mot RLS-drift)

- Drift-sjekk: `scripts/check-rls-drift.mjs` + `rls-drift-check.yml` (06:00 UTC + manuell); `DATABASE_URL` secret; `tests/rls/README.md`.
- Ikke PR-blokkerende; ingen auto-oppdatering av golden.

*Speil: `journal.txt` (rot).*

---

## 2026-05-16 — FASE 13.5-FIX-3-FOLLOWUP (fjern /api/orders/upsert)

- Slettet død rute `app/api/orders/upsert` (prod manglet `idem_get`/`idem_put`; kanon er POST `/api/orders` med `lp_idem_*`).
- Fjernet klient `upsertOrder` og hook `lib/orders/useOrderActions.ts`; kvitteringstyper for `ReceiptBanner` flyttet inn i komponenten (`OrderBannerAction`, `ReceiptState`, `ActionErrorState`).
- Oppdatert `lib/system/routeRegistry.ts` (fjernet `orders-upsert-post`), `lib/observability/sloRegistry.ts`, `docs/SLO_ALERTING_RUNBOOK.md`; `tests/api/order-api-guards.test.ts` uten upsert-describe.

*Speil: `journal.txt` (rot).*

---

## 2026-05-16 — FASE 13.5-FIX-5 (Migrasjonskollisjon 20260204)

- Diagnose: fem filer delte versjons-prefix 20260204 i `supabase/migrations/` (audit_events, company_archive, mega_motor phase1/2/3). Lokal `supabase db reset` feilet med 23505 på `schema_migrations` unique constraint. Prod hadde INGEN rader med `version='20260204%'` i `schema_migrations`, men hybrid objekt-tilstand (audit_events finnes, companies/profiles archive-kolonner finnes, mega_motor-tabeller finnes, men `company_deletions` + `claim_repair_jobs` + `repair_jobs_state_next_run_idx` manglet).
- Bruk-sjekk: `company_deletions` BRUKT av superadmin archive-summary route, `claim_repair_jobs` BRUKT av `superadmin/system/repairs/run`. Manglende objekter var faktiske bugs, ikke død kode.
- Fix:
  1. Rename 5 filer til unike timestamps (`20260204000001`–`5`) for å fjerne lokal kollisjon. Phase1+2 fikk samtidig BOM-fjerning pga Cursor Write-quirk.
  2. Ny migrasjon: `close_20260204_drift` med idempotent DDL for `company_deletions`, `claim_repair_jobs`, `repair_jobs_state_next_run_idx`.
  3. Apply mot prod via MCP (samme mønster som FIX-1/2). Verifisert alle tre objekter eksisterer i prod etter apply.
- Filnavn-justering: ny migrasjon renamet i repo til `20260516191414_close_20260204_drift.sql` for å matche prod `schema_migrations`-versjon.
- Workflow-status: `supabase-migrate.yml` vil ikke automatisk re-apply pga migration-gate blokkering på 20260513a/b (separat backlog). Prod er allerede synkronisert via MCP — workflow trenger ikke kjøre.
- Ingen selvrefererende SHA. Levert i denne commiten.

*Speil: `journal.txt` (rot).*
