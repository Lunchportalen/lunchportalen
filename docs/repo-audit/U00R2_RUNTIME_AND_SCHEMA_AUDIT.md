# U00R2 Runtime And Schema Audit

## Route / Schema Risk Map
| Route | Failure or risk | Evidence | Likely schema truth | Severity | Parity impact | Files to change next |
|---|---|---|---|---|---|---|
| `/api/backoffice/content/tree` | Tree route degrades when `content_pages` is missing `page_key`, `tree_parent_id`, `tree_root_key`, `tree_sort_order`, or when schema cache is stale. | `app/api/backoffice/content/tree/route.ts` contains degraded fallback logic; tests assert `degraded: true` behavior. | `content_pages` must expose tree columns as first-class schema, not just partial drift handling. | High | `DEGRADED` | `app/api/backoffice/content/tree/route.ts`, `ContentTree.tsx`, tree compatibility tests, relevant content-page migrations |
| `/api/backoffice/content/audit-log` | Route degrades to empty items when `content_audit_log` is missing or schema-misaligned; workspace history becomes weaker than route truth. | `app/api/backoffice/content/audit-log/route.ts` contains table/schema-degraded branches. | `content_audit_log` should exist with `page_id`, `variant_id`, `environment`, `locale`, `action`, `actor_email`, `metadata`, `created_at`. | High | `DEGRADED` | `app/api/backoffice/content/audit-log/route.ts`, history view/timeline files, `content_audit_log` migrations |
| `/api/backoffice/content/pages/[id]/variant/publish` | Route inserts `action: "content_publish"` into `content_audit_log`, but migrations only allow `workflow_change`, `publish`, `expire`, `workflow_blocked`, `release_execute`. | `app/api/backoffice/content/pages/[id]/variant/publish/route.ts` vs `20260229000001_content_audit_log_workflow.sql` and `20260304000001_content_audit_log_release_execute.sql`. | Either the route must write `publish`, or the DB check must explicitly allow `content_publish`. | Critical | `BROKEN` | Publish route and the two `content_audit_log` migrations |
| `/api/backoffice/releases` and `/api/backoffice/releases/[id]/execute` | Release execution depends on variant publish correctness and audit correctness; publish/audit drift leaks directly into release runtime. | Releases repo calls publish flow; release audit relies on same content lifecycle truth. | `content_releases`, `content_release_items`, `content_page_variants`, workflow state, and audit schema must align. | High | `PARTIAL` | `lib/backoffice/content/releasesRepo.ts`, release routes, publish route above |
| `/api/backoffice/settings` | Route already contains explicit fail-safe for missing `system_settings` table. Current evidence shows additive `killswitch` alignment, but no canonical create-table proof in this pass. | `app/api/backoffice/settings/route.ts` returns `SETTINGS_TABLE_MISSING`; `20260430120000_system_settings_killswitch.sql` is additive only. | `system_settings` must exist as a stable, canonical table before Settings becomes trustworthy. | High | `DEGRADED` | `app/api/backoffice/settings/route.ts`, `lib/system/settingsRepository.ts`, `lib/settings/getSettings.ts`, canonical `system_settings` migrations |
| `/api/backoffice/esg/latest-monthly` | Loader expects `delivered_count` and `cancelled_count`, but earlier migrations define `delivered_meals` and `canceled_meals`; later migration changes month type and field names. | `lib/esg/latestMonthlyRollupList.ts` vs `20260218_orders_rollup_invoice_esg_overview.sql`, `20260218_task8_10_esg_monthly_indices.sql`, `20260221_step6_10_fasit_periods_esg.sql`. | `esg_monthly` is schema-drifted in repo history; one canonical shape must be chosen and enforced. | High | `DEGRADED` | `lib/esg/latestMonthlyRollupList.ts`, backoffice/admin ESG routes, ESG migrations |
| `/api/backoffice/ai/intelligence/dashboard` | Route collapses failures to `INTEL_LOAD_FAILED` and does not expose a structured degraded payload comparable to tree/audit. | `app/api/backoffice/ai/intelligence/dashboard/route.ts` catches all failures; `ai_intelligence_events` migration exists. | `ai_intelligence_events` exists, but operator posture is weaker than content-tree/audit posture. | Medium | `PARTIAL` | Intelligence dashboard route and supporting intelligence loaders |
| `/api/backoffice/content/pages` | Create/list route is real, but its guarantees depend on `content_pages`, `content_page_variants`, and static document-type/create-policy truth staying aligned. | Route enforces document type and allowed-child logic; backed by `content_pages` tables and code-governed types. | Canonical truth must include stable tree columns, page metadata, and `documentType` / allowlist logic. | Medium | `PARTIAL` | `app/api/backoffice/content/pages/route.ts`, `contentDocumentTypes.ts`, `blockAllowlistGovernance.ts`, content-page migrations |
| `/api/backoffice/content/pages/[id]` | Save route depends on body-envelope and versioning shape; legacy and envelope modes still coexist. | Route reads/saves page body and versions; `page_versions` migration exists. | `content_page_variants.body` and `page_versions` must match current envelope/preview assumptions. | Medium | `PARTIAL` | `app/api/backoffice/content/pages/[id]/route.ts`, `bodyEnvelopeContract.ts`, `legacyEnvelopeGovernance.ts`, `page_versions` migration |
| `/api/backoffice/content/home` | Home bootstrap writes tree columns and immediately creates a prod variant; schema drift here fails hard instead of degrading. | Route writes `tree_root_key`, `tree_sort_order`, and a prod variant row. | Requires aligned `content_pages` tree columns and `content_page_variants` fields. | Medium | `PARTIAL` | `app/api/backoffice/content/home/route.ts`, tree migrations, variant migrations |
| `/api/backoffice/content/tree/move` | Move route is cycle-safe but not degraded; missing tree columns causes route failure. | Route updates `tree_parent_id`, `tree_root_key`, `tree_sort_order` directly. | Tree columns must be stable and always present. | Medium | `PARTIAL` | `app/api/backoffice/content/tree/move/route.ts`, tree migrations, move affordances in tree UI |
| `/api/content/global/settings` | Global settings route is real and persisted, but it is a parallel CMS truth outside the page-management API family. | `app/api/content/global/settings/route.ts` and `20260421000000_global_content.sql` prove persisted global settings. | `global_content` is canonical for header/footer/settings, separate from page tables. | Medium | `STRUCTURAL_GAP` | `app/api/content/global/settings/route.ts`, `readGlobal`/`writeGlobal` helpers, `global_content` migration |
| `/api/content/global/header` and `/api/content/global/footer` | Same persisted global-content contract exists, but not as Bellissima-managed entities with collection/workspace parity. | Routes read/save/publish against `global_content`. | `global_content` should remain the truth, but the management experience is still separate. | Medium | `STRUCTURAL_GAP` | Global header/footer routes and global content helpers |

## Audit Judgment
The editor is not failing because the API layer is fake. It is failing because the API layer is real and now collides with schema drift, partial degraded surfacing, and a few hard contract mismatches that Bellissima-style UX cannot hide.
