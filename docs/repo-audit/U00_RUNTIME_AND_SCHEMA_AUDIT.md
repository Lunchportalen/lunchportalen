# U00 Runtime And Schema Audit

## Route / Schema Risk Map
| Route | Failure or risk | Evidence | Likely schema truth | Severity | Parity impact | Files to change next |
|---|---|---|---|---|---|---|
| `/api/backoffice/content/tree` | Tree route degrades when `content_pages` is missing `page_key`, tree columns, or schema cache is stale. UX still depends on fallback roots rather than restoring canonical tree truth. | Route behavior from prior read, plus `tests/api/treeRouteDegradable.test.ts` returning `200` with `degraded: true` and `SCHEMA_OR_CACHE_UNAVAILABLE`. | `content_pages` must contain at least `id`, `title`, `slug`, `status`, `page_key`, `tree_parent_id`, `tree_root_key`, `tree_sort_order`. | High | `DEGRADED` | `app/api/backoffice/content/tree/route.ts`, `app/(backoffice)/backoffice/content/_tree/ContentTree.tsx`, `tests/api/treeRouteDegradable.test.ts` |
| `/api/backoffice/content/audit-log` | Audit route degrades to empty items when `content_audit_log` is missing or schema-misaligned; editor history still becomes operator-weaker than route truth. | Route behavior from prior read, plus `tests/api/contentAuditLogRoute.test.ts` expecting `degraded: true`, `TABLE_MISSING`, and `historyStatus: degraded`. | `content_audit_log` should exist with `page_id`, `variant_id`, `environment`, `locale`, `action`, `actor_email`, `metadata`, `created_at`. | High | `DEGRADED` | `app/api/backoffice/content/audit-log/route.ts`, `app/(backoffice)/backoffice/content/_components/ContentWorkspaceAuditTimeline.tsx`, `app/(backoffice)/backoffice/content/_components/ContentWorkspaceHistoryView.tsx` |
| `/api/backoffice/content/pages/[id]/variant/publish` | Route inserts `action: \"content_publish\"` into `content_audit_log`, but migrations only allow `workflow_change`, `publish`, `expire`, `workflow_blocked`, `release_execute`. | `app/api/backoffice/content/pages/[id]/variant/publish/route.ts` vs `20260229000001_content_audit_log_workflow.sql` and `20260304000001_content_audit_log_release_execute.sql`. | Either the route must write `publish`, or the DB constraint must explicitly allow `content_publish`. | Critical | `BROKEN` | `app/api/backoffice/content/pages/[id]/variant/publish/route.ts`, `supabase/migrations/20260229000001_content_audit_log_workflow.sql`, `supabase/migrations/20260304000001_content_audit_log_release_execute.sql` |
| `/api/backoffice/releases` + `/api/backoffice/releases/[id]/execute` | Release execution depends on variant publish correctness and audit-log correctness; a publish/audit mismatch leaks directly into release runtime. | `lib/backoffice/content/releasesRepo.ts` calls `publishVariant()` then inserts `release_execute`; publish path shares the variant publish assumptions. | `content_releases`, `content_release_items`, `content_page_variants`, workflow state, and audit schema must all align. | High | `PARTIAL` | `lib/backoffice/content/releasesRepo.ts`, `app/api/backoffice/releases/route.ts`, `app/api/backoffice/releases/[id]/execute/route.ts`, publish route files above |
| `/api/backoffice/settings` | Route already contains explicit fail-safe for missing `system_settings` table. Current migration evidence in this audit found only additive `killswitch` alignment, not a clear canonical create migration. | `app/api/backoffice/settings/route.ts` returns `SETTINGS_TABLE_MISSING`; current migration read found `20260430120000_system_settings_killswitch.sql` only. | `system_settings` must exist before backoffice settings can be trustworthy. | High | `DEGRADED` | `app/api/backoffice/settings/route.ts`, `lib/system/settingsRepository.ts`, `lib/settings/getSettings.ts`, `supabase/migrations/*system_settings*` |
| `/api/backoffice/esg/latest-monthly` | Loader expects `delivered_count` and `cancelled_count`, but other migrations define `delivered_meals` and `canceled_meals` with `month date`; one later migration uses text month plus count names. | `lib/esg/latestMonthlyRollupList.ts` vs `20260218_orders_rollup_invoice_esg_overview.sql`, `20260218_task8_10_esg_monthly_indices.sql`, `20260221_step6_10_fasit_periods_esg.sql`. | `esg_monthly` is currently schema-drifted in repo history; one canonical shape must be chosen. | High | `DEGRADED` | `lib/esg/latestMonthlyRollupList.ts`, `app/api/backoffice/esg/latest-monthly/route.ts`, `app/api/admin/esg/latest-monthly/route.ts`, relevant ESG migrations |
| `/api/backoffice/ai/intelligence/dashboard` | No obvious table-mismatch in current evidence, but route collapses all failures to `INTEL_LOAD_FAILED` and offers no degrade payload comparable to tree/audit. | `app/api/backoffice/ai/intelligence/dashboard/route.ts`; `lib/ai/intelligence/store.ts` and `20260323140000_ai_intelligence_events.sql` show real table presence. | `ai_intelligence_events` exists and is queried correctly, but operator posture is weaker than content tree/audit surfaces. | Medium | `PARTIAL` | `app/api/backoffice/ai/intelligence/dashboard/route.ts`, `lib/ai/intelligence/systemIntelligence.ts`, dashboard consumers |
| `/api/backoffice/content/home` | Home bootstrap inserts `tree_root_key` and immediately creates a prod variant. If tree columns or variant columns drift, route fails hard rather than degrading. | `app/api/backoffice/content/home/route.ts` writes `tree_root_key`, `tree_sort_order`, and inserts into `content_page_variants`. | Requires aligned `content_pages` tree columns and `content_page_variants` locale/environment/body fields. | Medium | `PARTIAL` | `app/api/backoffice/content/home/route.ts`, `supabase/migrations/20260317000001_create_content_pages_tables.sql`, tree migrations |
| `/api/backoffice/content/tree/move` | Move route is cycle-safe but not schema-degraded; missing tree columns yields route failure. | `app/api/backoffice/content/tree/move/route.ts` updates `tree_parent_id`, `tree_root_key`, `tree_sort_order` directly. | Tree columns must be present and placement constraints valid. | Medium | `PARTIAL` | `app/api/backoffice/content/tree/move/route.ts`, tree migrations, tree UI move affordances |
| `/api/backoffice/content/governance-usage` | Surface is operationally useful but capped, so it is not a complete audit source when repo or DB size grows. | `GovernanceInsightsPage` explicitly shows capped scanning; prior read of route showed capped variant scan. | Useful read-model, not exhaustive governance authority. | Medium | `PARTIAL` | `app/api/backoffice/content/governance-usage/route.ts`, `app/(backoffice)/backoffice/settings/governance-insights/page.tsx` |
| `/api/backoffice/content/pages` and `/api/backoffice/content/pages/[id]` | These are central and real, but they depend on the full `content_pages` / `content_page_variants` / `page_versions` shape staying aligned with body-envelope assumptions. | Prior route reads plus `tests/api/contentPages.test.ts`; migrations exist for pages, variants, and `page_versions`. | Canonical truth must include slug/title/status timestamps, locale/environment/body, and versioning support. | Medium | `PARTIAL` | `app/api/backoffice/content/pages/route.ts`, `app/api/backoffice/content/pages/[id]/route.ts`, `supabase/migrations/20260317000001_create_content_pages_tables.sql`, `20260430100000_page_versions.sql` |

## Route Judgment
The editor is not failing because the API layer is fake. It is failing because the API layer is real and now colliding with schema drift, partial degrade surfacing, and a few contract mismatches that Bellissima-style UX cannot hide.
