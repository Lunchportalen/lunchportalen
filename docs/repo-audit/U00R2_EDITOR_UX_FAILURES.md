# U00R2 Editor UX Failures

Screenshots were not attached in this session. The table below therefore uses code structure and mounted surface composition as evidence for likely workflow failures.

| Problem | Where seen | Severity | Why it hurts workflow | Bellissima concept violated | Likely owning files |
|---|---|---|---|---|---|
| Too much stacked chrome before the editor body | Bellissima header + local topbar + publish bar + footer apps | High | Editors spend vertical space parsing multiple status/action rows before they reach the real canvas. | `Workspace Context`, `Workspace Actions`, calm one-workspace composition | `ContentWorkspaceHost.tsx`, `BellissimaWorkspaceHeader.tsx`, `ContentWorkspaceHeaderChrome.tsx`, `ContentWorkspacePublishBar.tsx`, `BackofficeWorkspaceFooterApps.tsx` |
| Tree primacy weakens once entity workspace opens | Left tree remains mounted, but entity chrome and preview concepts dominate attention | High | The tree is supposed to be primary navigation; in practice the workspace chrome becomes the loudest structure. | `Tree-first section model`, `Collections / Workspaces` | `ContentWorkspaceHost.tsx`, `ContentTree.tsx`, `ContentWorkspacePageEditorShell.tsx` |
| `/backoffice/content` still feels like a landing page beside the editor, not the same continuous workspace system | Section landing with cards/recent pages/create action | Medium | It introduces another mode before editing, which can feel like a hub instead of a collection root. | `Section -> collection -> workspace` continuity | `content/page.tsx`, `ContentSectionLanding.tsx`, `backofficeWorkspaceContextModel.ts` |
| Preview has three competing meanings | Inline `LivePreviewPanel`, workspace preview view, dedicated `/backoffice/preview/[id]` route | High | Editors must infer when they are seeing inline draft, dedicated draft, or public/published comparison. | `Workspace Views`, `Preview as first-class workspace` | `ContentWorkspaceMainCanvas.tsx`, `LivePreviewPanel.tsx`, `PreviewCanvas.tsx`, `app/(backoffice)/backoffice/preview/[id]/page.tsx` |
| Inspector grouping is still noisy and mixed-scope | Right rail mixes page/global/design/document type/runtime/AI controls | High | Too many scopes are collapsed into one inspector experience, reducing confidence and scan speed. | `Property Editor UI`, coherent inspector/workspace grouping | `RightPanel.tsx`, `ContentWorkspacePropertiesRail.tsx`, `ContentWorkspace.tsx` |
| The main editor does not fully read as one calm workspace | Monolithic state + tri-pane + inline preview + AI + history + global/design modes all converge in one owner | High | A single workspace exists technically, but it still feels operationally overstuffed. | `Workspace Context`, single coherent work area | `ContentWorkspace.tsx`, `ContentWorkspacePageEditorShell.tsx`, `ContentWorkspaceMainCanvas.tsx` |
| Block discovery is split across two entry patterns | `BlockAddModal` vs `BlockPickerOverlay` | Medium | Editors get both a quick add modal and a rich picker overlay instead of one clear discovery model. | `Entity Create Option Action`, calm discovery | `BlockAddModal.tsx`, `BlockPickerOverlay.tsx`, `ContentWorkspaceModalStack.tsx` |
| Settings looks more mature than it is | “First-class section” language, collection/workspace IA, but mostly static registry reads | Medium | It raises expectations of CRUD-like schema management that the runtime does not yet provide. | `Data Types`, `Property Editor Schema`, `Management API` | `settings/page.tsx`, `document-types/*`, `data-types/*`, `create-policy/page.tsx`, `backofficeSchemaSettingsModel.ts` |
| Document types and data types do not yet feel like trustworthy managed objects | Only one real document type; data types are explained rather than managed | High | Editors/admins cannot build a strong mental model of a type system that is mostly descriptive. | `Document Types`, `Data Types`, `Property Editors` | `contentDocumentTypes.ts`, `backofficeSchemaSettingsModel.ts`, settings pages |
| History degradation is not elevated enough in the workspace | Tree/audit APIs degrade honestly, but history still competes with generic workspace noise | Medium | Operational truth is available, but not promoted strongly enough when degraded. | `Workspace Footer Apps`, `History workspace` | `ContentWorkspaceAuditTimeline.tsx`, `ContentWorkspaceHistoryView.tsx`, `BellissimaWorkspaceHeader.tsx`, `BackofficeWorkspaceFooterApps.tsx` |
| Global, design, and page modes still bleed into one editor personality | Explicit Bellissima views exist, but all route back into the same heavy workspace owner | Medium | The editor changes labels/views, yet the underlying interaction model does not simplify enough per scope. | `Workspace Views`, `Global Context` | `ContentWorkspace.tsx`, `ContentWorkspacePageEditorShell.tsx`, `backofficeWorkspaceContextModel.ts` |
| AI/governance/runtime side surfaces risk editor noise | Multiple panels and control-plane affordances can sit beside core editing | Medium | The editor can stop feeling like an authoring surface and start feeling like a diagnostic cockpit. | `Calm workspace`, `one dominant task` | `ContentWorkspace.tsx`, `useContentWorkspaceAi.ts`, right-rail surfaces, backoffice AI routes |

## UX Judgment
The editor does not primarily suffer from missing surfaces. It suffers from too many real surfaces competing at once. Bellissima-like structure exists, but the experience is still too fragmented between tree, stacked chrome, inspector scope, and preview modes.
