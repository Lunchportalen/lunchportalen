# CP4 — Next steps

1. **Worker** — implementer eller dokumenter eksplisitt deaktivering.
2. **Studio deep-links** — hvis stabile paths finnes, legg til i publish panel.
3. **Optional** — `RuntimeDomainLinkCard` kan fjernes fra repo om ubrukt (fremtidig opprydding).

Fase CP4 stoppet — ingen nye produktfaser uten eksplisitt instruks.
