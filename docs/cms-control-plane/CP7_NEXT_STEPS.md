# CP7 — Next steps (ikke startet her)

1. Drift: bekreft `SANITY_WRITE_TOKEN` i alle miljøer som skal bruke broker.
2. Dokumenter synk mellom `menu_visibility_days` og Sanity hvis begge er aktive i samme operasjon.
3. Vurder liten test-suite med mock av Sanity-klient for `publishMenuContentDraftForDate` (valgfritt).
4. Vurder UX: liste kommende ukedager med publish-knapper (kun etter produkt-OK).

**Stopp:** Ingen nye produktfaser uten egen instruks.
