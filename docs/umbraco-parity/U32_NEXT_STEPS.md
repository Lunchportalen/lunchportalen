# U32 - Next steps

1. Add browser-level smoke coverage for `tree -> workspace -> history -> preview` in the content vertical.
2. Decide explicitly whether document types and data types stay code-governed or move to a persisted management workflow.
3. Normalize remaining legacy `page` / `editor` naming so code, tests, docs, and UI all speak the same workspace language.
4. Extend the shared workspace/action/footer model into the next adjacent management module instead of letting content become a one-off strong vertical.
5. Validate `sanity:live` against a reachable running environment when preparing the next release gate.
