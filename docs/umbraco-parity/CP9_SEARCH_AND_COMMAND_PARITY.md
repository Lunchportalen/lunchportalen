# CP9 — Search and command parity (WS4)

## Implementert

- **Content tree:** klientfilter + auto-expand til treff (`treeMock.ts` + `ContentTree.tsx`).

## Planlagt (ikke CP9)

- Global palett / server-side indeks — se `CP9_BACKOFFICE_SEARCH_AND_COMMANDS_PLAN.md`.
