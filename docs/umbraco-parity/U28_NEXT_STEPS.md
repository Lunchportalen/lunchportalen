# U28 — Next steps (ikke påbegynt)

1. E2E: superadmin logger inn og kjører dry-run batch (Playwright) — ved behov for regressjon.
2. Vurder `company_admin` read-only governance insights med tenant-filter — egen godkjenning.
3. Harmoniser én discovery-rad med `backofficeEntityActionPrimaryClass` hvis UX ønsker visuell paritet (lav prioritet).

**Stopp:** ingen nye produktfaser uten eksplisitt instruks.
