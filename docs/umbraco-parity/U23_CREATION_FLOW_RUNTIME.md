# U23 — Creation flow runtime

## Oppførsel

- **`/backoffice/settings/create-options`**: Lesbar forklaring av tre-create vs blokk-create; tydeliggjør at blokkliste **ikke** er filtrert per dokumenttype uten videre policy.
- **ContentWorkspaceCreatePanel**: Tekstfiks for dokumenttype-knapper («Opprett en ny side med typen …»).

## Ikke levert (bevisst)

- Server-side block allowlist per `documentType` — replatforming/produkt.
