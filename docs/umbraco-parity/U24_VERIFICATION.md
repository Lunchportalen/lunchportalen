# U24 — Verifikasjon

```bash
npm run typecheck
npm run build:enterprise
npm run test:run
```

**Kjørt:** alle tre — `typecheck` OK, `build:enterprise` OK (exit 0), `test:run` OK (227 filer, 1248 tester).

**Fokus:** `tests/cms/blockAllowlistGovernance.test.ts`, `tests/backoffice/documentTypes.test.ts`, CMS content workspace smoke.
