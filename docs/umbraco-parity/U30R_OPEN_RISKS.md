# U30R — Open risks

1. Miljøer som mangler `tree_*` kolonner trenger egne migrasjoner — ikke dekket av kun `page_key`.
2. Slug-inferens dekker ikke alle tilpassede slug-er — kan mangle `kind` for spesialtilfeller.
