# U24 — Creation flow runtime

## Blokker

- **Modal** (`BlockAddModal`): filtrert liste etter `allowedBlockTypeKeys`.
- **Block picker**: `BLOCK_REGISTRY` filtrert mot samme nøkler.
- **Server**: se `U24_DOCUMENT_TYPE_RUNTIME.md`.

## Tre

- Uendret: `allowedChildTypes` fra eksisterende create-flyt.
