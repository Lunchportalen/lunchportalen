# U30 — Next steps

1. Kjør `npm run build:enterprise` og full `npm run test:run` i CI.
2. Legg ved reelle skjermbilder i `U30_EDITOR_FAILURES_FROM_SCREENSHOT.md` og juster spacing hvis nødvendig.
3. Vurder inspector `<details>`-grupper per fane (lav risiko, egen PR).
4. Produkt: vurder om `GET /api/backoffice/content/tree` skal inkludere flere roller (stort grep — ikke start uten eksplisitt instruks).
