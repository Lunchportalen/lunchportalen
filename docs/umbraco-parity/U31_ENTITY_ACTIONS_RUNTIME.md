# U31 — Entity actions

- **Tree:** `TreeNodeRow` + `NodeActionsMenu` — preview, copy link, create, rename, move (policy-bound).
- **Lists:** `backofficeEntityActionStyles` — primary/muted classes for collection rows.
- **No new mutation engine** — same API routes as before.
