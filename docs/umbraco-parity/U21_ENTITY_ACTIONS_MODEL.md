# U21 — Entity actions

## I dag

- **Discovery:** palett med manifest + U20 entiteter (`u20id` for media).
- **Domain surfaces:** `CONTROL_PLANE_DOMAIN_ACTION_SURFACES[].actions`.
- **Trees:** content tree actions i eksisterende komponenter.

## U21

- Workspace **toolbar / secondaryActions** som **kontekstuelle** lenker (view/edit/preview/runtime) — samme mønster som entity actions, uten ny mutasjonsmotor.

## Ikke-mål

- Nye server-actions parallelt med eksisterende API.
