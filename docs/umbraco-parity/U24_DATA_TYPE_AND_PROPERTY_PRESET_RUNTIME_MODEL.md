# U24 — Data type og property preset runtime

## U24-avgrensning

- **Data type / property preset** forblir primært i `blockFieldSchemas.ts` + design presets — U24 **utvider ikke** persisted preset-database.
- **Forbedring**: tydelig kobling dokumenttype → tillatte blokker → blokkenes felt-schema (allerede i modal via PropertyDatasetExplainer).

## UX vs teknisk

- Full Umbraco **property value preset**-butikk i DB = replatforming — se `U24_REPLATFORMING_GAPS.md`.
