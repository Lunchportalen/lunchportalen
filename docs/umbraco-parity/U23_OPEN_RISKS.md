# U23 — Åpne risikoer

1. **Drift mellom `EditorFieldKind` og `EDITOR_FIELD_KIND_GOVERNANCE`** — manuell synk ved nye felt-typer.
2. **Bruker forventer redigerbare typer i UI** — mitigeres med tydelig «read-only / kildekode»-tekst på schema-siden.
3. **Deep links** til `/backoffice/settings` fra bokmerker til gammel «kun system»-side — nå hub; system ligger på `/settings/system` (akseptabel breaking UX for superadmin).
