# CP13 — Open risks

1. **Prefix-match aktiv fane** — ekstremt edge case hvis to nav-href er prefiks av hverandre (ikke observert i dagens liste).
2. **Manifest kun i kode** — krever deploy for å endre moduler (akseptert).
3. **Workspace session types** — ikke tvunget på alle sider — inkonsistent inntil gradvis adopsjon.
