# U31 — Hardening

- Tree empty state messaging (operator clarity).
- Audit degradation visible in workspace footer when API reports degraded (superadmin).
- TopBar overflow prevents unbounded horizontal module sprawl.

**REPLATFORMING_GAP:** Full Umbraco Management API / persisted datatype store not in scope; parity is UX/control-plane on current stack.
