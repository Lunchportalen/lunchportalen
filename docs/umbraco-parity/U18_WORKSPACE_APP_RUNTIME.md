# U18 — Workspace app consistency (runtime)

## U18

- Felles **historikk-strip** og **context strip** i `BackofficeShell` gir tverrgående krom.
- AI-siden følger samme typografi (`PageContainer`, `rounded-xl` cards).

## Neste (utenfor U18)

- Systematisk migrering av alle workspaces til identisk toolbar — egen iterasjon.
