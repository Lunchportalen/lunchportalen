# U30R — Settings runtime

- Hub-side (`settings/page.tsx`) redesignet med ikoner og tydelig hierarki.
- `SettingsSectionChrome`: bredere sidenav (`lg:w-72`), skygge.
- `BACKOFFICE_SETTINGS_EXTENSION_ID` i registry for konsistent manifest-referanse.
