# U31 — Content workspace runtime

- **Shell:** `SectionShell` wide tree column; `ContentWorkspaceShell` **U31:** `minmax(360px, 520px)` tree column.
- **Canvas vs preview:** `ContentMainShell` **U31:** `lg:grid-cols-[1fr_1.45fr]` when split — preview column larger.
- **Live preview panel:** `LivePreviewPanel` **U31:** wider `max-w-2xl` container for preview card.
- **Header:** fewer competing controls in top row (diagnostics in overflow).
