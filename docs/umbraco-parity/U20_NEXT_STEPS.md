# U20 — Next steps (ikke startet her)

1. Valgfritt: les `u20id` i `media/page.tsx` for scroll/highlight.
2. Vurder scope-utvidelse for discovery (kun hvis produkt/krav endres).
3. Kjør `sanity:live` i miljø med faktisk `PUBLIC_APP_URL` / health.
