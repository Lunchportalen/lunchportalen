# U26 — Allowlist runtime

- **Save:** uendret (PATCH + `validateBodyPayloadBlockAllowlist`).
- **Duplicate:** U25 klientguard.
- **U26 AI:** `validateEditorBlockTypesForGovernedApply` før patch brukes i editor når `documentTypeAlias` er satt.
