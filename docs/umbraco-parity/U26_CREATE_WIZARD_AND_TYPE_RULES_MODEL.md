# U26 — Create wizard and type rules

- **Create panel:** uendret logikk (allowedChildTypes, `submitCreateContentPageFromForm`).
- **Type rules:** dokumenttype i registry styrer allowlist ved lagring når envelope finnes.
- **U26:** tydeligere **legacy vs kanonisk** i egenskaps-rail + management read-siden for overordnet innsikt.

Ingen ny policy-motor i U26.
