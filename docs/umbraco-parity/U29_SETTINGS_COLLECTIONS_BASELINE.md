# U29 — Settings collections baseline

**Før U29:** Hub med kortlenker; schema-side med tabeller; ingen dedikert collection/workspace for document type eller data type rad.

**Etter U29:** Sidenav + document types collection + per-alias workspace; data types collection + per-kind workspace; create policy workspace; governance/management/system uendret i sannhet.

**Kilde:** `backofficeSchemaSettingsModel`, `contentDocumentTypes`, nye routes under `app/(backoffice)/backoffice/settings/`.
