# U24 — Settings runtime

- **Schema-siden** (`/backoffice/settings/schema`): viser kolonne «Tillatte blokker» per dokumenttype.
- **Create-options-siden**: beskriver U24-håndheving og legacy-unnatak.
- **Persistence**: fortsatt kode-registry — se `U24_SETTINGS_PERSISTENCE_DECISION.md`.
