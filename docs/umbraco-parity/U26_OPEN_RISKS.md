# U26 — Open risks

1. Brukere kan utsette lagring etter «Oppgrader» — fortsatt kladd til lagring skjer.
2. AI preflight sjekker **blokktyper**; full body-byte-identitet med server valideres ved lagring.
3. `ContentMainShell.tsx` har fortsatt legacy-tekst (fil vurderes ubrukt i mount); **sanntids** UI er `ContentWorkspacePropertiesRail`.
