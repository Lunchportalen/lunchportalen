# CP8 — Menu publish parity runtime (WS3)

## Operativ kjede (sannhet i kode)

- `OPERATIONAL_WEEK_MENU_PUBLISH_CHAIN` — steg 2 beskriver nå **Studio og/eller in-CMS broker** (samme Sanity Actions).
- UI: `CmsOperationalPublishChain` hjelpetekst oppdatert.

## Paneler på `/backoffice/week-menu`

- Readiness, native publish (`menuContent` per dato), Studio-handoff.
- **weekPlan** fortsatt merket editorial-only i eksisterende side.

## Ikke gjort

- Bulk publish for hele uker (risiko / produkt).
