# U29 — Data type collection and preset model

- **Data types:** `EDITOR_FIELD_KIND_GOVERNANCE` — collection på `/settings/data-types`, detail på `/settings/data-types/[kind]`.
- **Presets:** fortsatt implisitt i `blockFieldSchemas` / editor — ikke persisted instances; U29 gjør kind-rolle **eksplisitt** i workspace.
