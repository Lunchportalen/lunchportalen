# CP8 — weekPlan editorial parity (WS3)

## Sannhet

- **weekPlan** er **ikke** `GET /api/week` for ansatte.

## UI

- Amber panel + eksplisitt språk på week-menu siden (eksisterende).

## CP8

- Ingen endring av weekPlan-schema; **kun** dokumentasjon og publish-kjede-fokus på `menuContent`.
