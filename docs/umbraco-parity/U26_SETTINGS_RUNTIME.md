# U26 — Settings runtime

- Hub: `/backoffice/settings` + ny kortlenke til **Management read**.
- **management-read:** server-renderet liste fra `backofficeSchemaSettingsModel`.
- **governance-registry API:** samme data som JSON (`source: "code_registry"`).
