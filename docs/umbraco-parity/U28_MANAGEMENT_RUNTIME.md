# U28 — Management runtime

- **GET** `governance-usage` utvidet med **governance coverage:** `governedAllowlistOk`, `governedAllowlistFail`, `invalidDocumentTypeVariants` (fra `validateBodyPayloadBlockAllowlist` per variant).
- **Preset-impact:** fortsatt indirekte via blokk- og dokumenttype-telling — ingen ny preset-tabell.
