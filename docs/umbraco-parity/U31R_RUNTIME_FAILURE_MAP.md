# U31R Runtime Failure Map

| Route | Current failure | Likely cause | Fix now? | Degrade honestly? | Files to touch |
| --- | --- | --- | --- | --- | --- |
| `/backoffice/content` | Section root is acting like a detail route and currently has broken imports in IDE. | Route ownership drift between section overview and `[id]` detail handling. | Yes | N/A | `app/(backoffice)/backoffice/content/page.tsx`, `app/(backoffice)/backoffice/content/_workspace/ContentDashboard.tsx`, `tests/backoffice/content-page-smoke.test.tsx` |
| `/api/backoffice/content/tree` | IDE type error on `fetched.error`; runtime schema drift is already partially degraded but messaging is still fragmented between API and tree UI. | Discriminated-union narrowing is not explicit enough for TypeScript; DB schema can drift on `page_key`, tree columns, or schema cache. | Yes | Yes | `app/api/backoffice/content/tree/route.ts`, `app/(backoffice)/backoffice/content/_tree/mapTreeApiRoots.ts`, `app/(backoffice)/backoffice/content/_tree/ContentTree.tsx`, `tests/api/treeRouteDegradable.test.ts`, `tests/cms/mapTreeApiRoots.test.ts` |
| `/api/backoffice/content/audit-log` | Prompted failure mode is missing table/schema cache. Current route already returns degraded 200, but the editor still treats that state as a generic warning instead of a first-class operator status. | `content_audit_log` can be missing, stale in schema cache, or missing columns; degraded status is not surfaced clearly enough in history/runtime UX. | Yes | Yes | `app/api/backoffice/content/audit-log/route.ts`, `app/(backoffice)/backoffice/content/_components/useContentAuditLogHealth.ts`, `app/(backoffice)/backoffice/content/_components/ContentWorkspaceAuditTimeline.tsx`, `app/(backoffice)/backoffice/content/_components/ContentWorkspaceHistoryView.tsx`, `tests/api/contentAuditLogRoute.test.ts`, `tests/cms/auditLogCompat.test.ts` |
| `/backoffice/content/[id]` | Editor opens, but the workspace is still too fragmented: top chrome is dense, tree primacy is weak, preview is too constrained, inspector grouping is noisy, and history/runtime cues are split. | Bellissima parity is only partially applied across shell, tri-pane grid, history, footer, and settings context. | Yes | Yes | `BackofficeShell`, `SectionShell`, `TopBar`, `ContentWorkspace*` layout files, `RightPanel`, `ContentWorkspacePropertiesRail`, `BackofficeWorkspaceFooterApps` |
| `/backoffice/settings` | No hard runtime crash, but settings is still visually secondary relative to content and not yet operating like a first-class section/workspace flow. | Settings still mixes hub cards, shell chrome, and side navigation without a single strong section hierarchy. | Yes | Yes | `app/(backoffice)/backoffice/settings/layout.tsx`, `app/(backoffice)/backoffice/settings/page.tsx`, `app/(backoffice)/backoffice/settings/_components/SettingsSectionChrome.tsx`, `components/backoffice/SettingsCollectionShell.tsx` |

## Runtime Notes

- The latest captured enterprise build was green, but current IDE diagnostics still show broken imports in `content/page.tsx` and the tree-route narrowing error.
- Tree and audit already contain real degraded logic. U31R should harden and surface that logic, not replace it with a parallel motor.
