# U30 — Backoffice IA runtime

- Global **TopBar** (U29R) uendret i U30 — fortsatt seksjon som `<select>` + modulrad.
- **Reduksjon av visuell støy** skjedde i **content workspace** ved å fjerne duplikat status i `ContentWorkspaceEditorModeStrip` (status ligger i `ContentTopbar`).
