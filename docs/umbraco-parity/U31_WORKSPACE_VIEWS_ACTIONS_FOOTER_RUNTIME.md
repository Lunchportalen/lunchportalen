# U31 — Workspace views / actions / footer

- **Views:** `BackofficeWorkspaceViewTabs` on content landing & sub-routes (overview, growth, recycle-bin).
- **Primary actions:** `ContentTopbar` — **U31:** `Publiser` / `Sett til kladd` primary; secondary (`Kopier support`, retry, reload) under **«Diagnose og mer»** (`BackofficeOverflowActionBar`).
- **Footer apps:** `BackofficeWorkspaceFooterApps` — persistent strip: publish/dirty/type, governance links, preview link, audit degrading badge when applicable.
